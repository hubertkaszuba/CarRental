@model IEnumerable<CarRental.Models.Rent>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCarNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoginNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCarNavigation.IdCar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoginNavigation.Login)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRent">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRent">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRent">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
